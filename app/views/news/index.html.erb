<!DOCTYPE html>
<html>
  <head>
    <% content_for :title, "Fid | News" %>
    <title>News</title>
<style>
body {
  font-family: "Roboto", sans-serif;
  margin: 0 auto;
  /* padding: 0 1rem; */
  max-width: 60em; 
  line-height: 1.5;
}
small {
  display: block;
}

h1 {
  text-align: center;
}

h3 {
  text-align: center;
}

nav {
  text-align: center;
  margin: 1rem 0;
  padding: 0.5rem;
  /* background: #f5f5f5; */
}

nav a {
  margin: 0 1rem;
  text-decoration: none;
  color: #333;
  font-weight: bold;
}

nav a:hover {
  color: #0066cc;
}

nav a.active {
  color: #0066cc;
  border-bottom: 2px solid #0066cc;
}

.pagination {
  text-align: center;
  margin: 2rem 0;
  padding: 1rem;
}

.pagination a {
  color: #0066cc;
  text-decoration: none;
  padding: 0.5rem 1rem;
  font-weight: bold;
}

.pagination a:hover {
  text-decoration: underline;
}

.pagination .disabled {
  color: #999;
  padding: 0.5rem 1rem;
}

</style>
  </head>
  <body>
<nav>
  <%= link_to "Feed", fid_index_path %>
  <%= link_to "News", news_index_path, class: "active" %>
</nav>

<h1>News</h1>
<ol>
<% @feed.each do |e| %>
  <li><a href="<%= e.url %>" target=<%= "_blank_#{e.id}"%>"><%= e.title %></a>
    <small><%= e.author || e.url %> | <%= distance_of_time_in_words(Time.now, e.published) %></small></li>
<% end %>
</ol>

<%= pagination_links(@pagination, :news_index_path) %>

  </body>
</html>
